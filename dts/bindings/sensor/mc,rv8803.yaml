description: Micro Crystal RV8803 real-time clock driver sensor

compatible: "mc,rv8803"

include: ["i2c-device.yaml", "sensor-device.yaml"]

properties:
  reg:
    required: true
    description: device address on i2c bus